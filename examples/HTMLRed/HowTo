1. Install Hammer

in McHammerCoder/core/Hammer
> mvn clean install

2. Build Monticore

in McHammerCoder/core/Monticore
> mvn clean package 

3. Run the run.sh to start code generation:

> sh codegen.sh

4. Run compile.sh to compile the generated example code:

> sh compile.sh

5. Compile the ./out/grammarName/_mch_parser/com_upstandinghackers_hammer_Hammer.c (Soon automated)
   A compiled version for HTMLRed already exists in this folder: libjhammer_actions.so

make REALLY_USE_OBSOLETE_BUILD_SYSTEM=yes

6. Run run.sh to execute the example:

> sh run.sh
